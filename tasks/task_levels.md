# Task: Levels & Content

- Create `game/data/levels.json` with at least 3 levels.
- Add schema validation in `levels.py` with helpful errors.
- Each level lists enemy kinds and optional reward points.

Acceptance:
- Loading invalid JSON or schema prints a clear error and exits gracefully.
