# TODO: define GameState (dataclass) with fields like running, tick, player_hp
# TODO: define GameEngine class with methods: update(), is_running(), stop()

# Keep this file minimal; implement during the course.
